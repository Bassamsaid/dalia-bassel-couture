# Dalia Bassel Couture — real backend (Node.js, zero dependencies)
Start: npm start  (Node 22+). Default admin: admin@daliessa.com / daliessa123
Railway: runs `npm start` automatically. For persistent data attach a Volume
mounted at /data and set env DATA_DIR=/data and UPLOAD_DIR=/data/uploads.
